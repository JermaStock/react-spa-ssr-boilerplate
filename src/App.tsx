import React from 'react';
import { hot } from 'react-hot-loader/root';

const AppComponent = () => {
  return (
    <div>
      content
    </div>
  );
};

export const App = hot(AppComponent);